# Http Host

Fetch `Request → Response` 路由表、`createFetchApp`（Bearer、CORS、JSON body）、Node `serveFetch` 适配。

- 官方插件路由：`registerFetchRoute(table, method, path, handler)`
- 遗留：`Router` compat shim（**deprecated**，见 ADR-0009）
- ADR：[docs/adr/0008-host-edge-remote-console.md](../../docs/adr/0008-host-edge-remote-console.md)
