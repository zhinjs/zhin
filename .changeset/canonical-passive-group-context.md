---
'@zhin.js/agent': patch
---

Replace Message-based passive group context APIs with canonical session/principal observations, remove the deprecated append API, and make TurnIngress session preparation drain passive context by its fixed session identity.
