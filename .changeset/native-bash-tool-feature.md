---
"@zhin.js/agent": minor
"@zhin.js/cli": patch
---

Move Bash execution to a native generation ToolFeature backed by an explicit execution port and the typed Turn policy. Remove the classic Bash tool API, hidden input authority channel, and `createBuiltinTools` Bash entry.
