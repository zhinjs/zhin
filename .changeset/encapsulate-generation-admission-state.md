---
"@zhin.js/plugin-runtime": patch
---

Encapsulate generation admission ownership, activation state, and lifecycle listeners in each `GenerationAdmissionGate` instance while preserving atomic snapshot publication.
