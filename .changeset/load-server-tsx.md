---
"@zhin.js/runtime": patch
"@zhin.js/cli": patch
"create-zhin-app": patch
---

Load server TSX features through the active process loader or the CLI's scoped fallback loader, preserve explicit loaders across supervised restarts, and scaffold the JSX compiler settings advertised by the feature conventions.
