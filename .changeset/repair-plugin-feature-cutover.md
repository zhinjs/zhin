---
"@zhin.js/cli": patch
---

Make package cutover add the Feature declarations and direct provider dependencies required by discovered capability directories in existing plugins, while preserving customized plugin entry files.
