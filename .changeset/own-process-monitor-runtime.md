---
"@zhin.js/process-monitor": patch
---

Replace module-global process monitoring with an owner-scoped `ProcessMonitor` resource consumed by the command and Agent tool. Configuration and documentation now expose only implemented Webhook notifications.
