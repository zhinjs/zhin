---
'@zhin.js/cli': patch
---

Split Console entry, system, plugin, path, and runtime snapshot projections into focused modules, and replace legacy plugin detail aliases and empty context placeholders with the canonical `packageRoot` field.
