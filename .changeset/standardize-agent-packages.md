---
"@zhin.js/agent-feature": minor
"@zhin.js/agent": minor
"@zhin.js/feature-kit": minor
"@zhin.js/runtime": minor
"create-zhin-app": minor
"@zhin.js/plugin-lottery": minor
---

Make root `AGENTS.md` the main Agent contract and `agents/<name>/agent.json` plus `system.md`, `boundaries.md`, and `conventions.md` the only named sub-agent authoring shape. Remove flat Markdown and `agent.ts` discovery, project Agent metadata and resources through one parser, enforce access predicates and governed Tool allowlists, and keep supporting-file changes on Agent Slot hot reloads.
