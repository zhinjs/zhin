---
'@zhin.js/cli': patch
---

Make the IM Agent route consume Host tools only through the fixed-generation capability projection. Remove the duplicate route-local `extraTools` and Bash assembly path.
