---
"@zhin.js/adapter-github": patch
---

Require one canonical expanded GitHub endpoint configuration. Remove nested endpoint, environment, camelCase alias, default identity, and inert polling compatibility while making the resolved App credentials and workspace root explicit.
