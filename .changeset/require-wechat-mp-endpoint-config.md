---
"@zhin.js/adapter-wechat-mp": patch
---

Require one canonical expanded WeChat MP endpoint configuration. The protocol no longer reinterprets nested `endpoints` or reads endpoint identity and credentials from `process.env`.
