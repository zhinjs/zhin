---
'@zhin.js/cli': patch
---

Separate Console configuration documents, environment files, and plugin schema conversion behind a small project store facade. Emit only the canonical `object` field for converted object schemas.
