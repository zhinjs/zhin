# Optional example only — not wired to zhin CI.
# Replace the image and commands with your own build (BYO).

services:
  # zhin:
  #   image: your-registry/zhin-bot:local
  #   restart: unless-stopped
  #   env_file:
  #     - .env.bridge.local   # gitignored; defines ZHIN_BRIDGE_IPC_TOKEN etc.
  #   # volumes / ports per your adapters

  _placeholder:
    image: alpine:3.20
    command: ["sh", "-c", "echo 'Edit this file: remove _placeholder and uncomment zhin service with your image.' && sleep 2"]
    profiles:
      - donotstart
