# 由 zhin.config.yml 引用（ConfigLoader 环境变量替换）
HTTP_TOKEN=

OPENAI_API_KEY=
OPENAI_BASE_URL=

# 本地可省略（使用 zhin.config.yml 的 sqlite）。Deno Deploy / 生产请用托管 PostgreSQL：
# DATABASE_URL=postgres://user:pass@host:5432/dbname?sslmode=require
# DATABASE_POOL_MAX=5
# HTTP_TRUST_PROXY=1
