# CI contract fixture without credentials. It validates Agent bindings and Kernel task tools.

ai:
  trigger:
    prefixes:
      - "ai:"
  agents:
    planner:
      provider: ollama
      model: placeholder
    researcher:
      provider: ollama
      model: placeholder
  agent:
    tools:
      - spawn_task
