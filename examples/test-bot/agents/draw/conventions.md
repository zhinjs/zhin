# Conventions

Use the configured provider and model defaults, translate the request into a concrete prompt, match the requested style, and return a short caption without base64 or placeholders. Add recurring role-specific mistakes to this file.
