{
  "name": "multi-agent-room",
  "private": true,
  "version": "0.0.25",
  "description": "单 Bot 多 Agent 协作示例（Workroom Kernel + Agent bindings）",
  "type": "module",
  "scripts": {
    "dev": "zhin runtime start",
    "start": "zhin runtime start --mode production --no-watch",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@zhin.js/adapter-sandbox": "workspace:*",
    "@zhin.js/agent": "workspace:*",
    "ai": "^6.0.238",
    "zhin.js": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@zhin.js/cli": "workspace:*",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "zhin": {
    "protocol": 1,
    "type": "plugin",
    "entry": "./plugin.ts",
    "engine": "^1.0.0",
    "runtime": "trusted",
    "features": [],
    "plugins": [
      {
        "package": "@zhin.js/adapter-sandbox",
        "instanceKey": "sandbox"
      }
    ]
  }
}
