# Web 控制台 / HTTP API（与 zhin.config 中 http.token 一致）
HTTP_TOKEN=minimal-dev-token

# 可选：使用 OpenAI 兼容 API 时取消注释并改 ai.defaultProvider
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1

# 默认使用本地 Ollama（ai.providers.ollama.host）
# 需先安装并运行：ollama serve
