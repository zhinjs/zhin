import { Contact } from "../entries/contact";
import { Client } from "../client";
export declare class Member extends Contact {
    group_id: string;
    user_id: string;
    nickname: string;
    get is_self(): boolean;
    get group(): import("./group").Group;
    constructor(c: Client, info: Member.Info);
    static from(this: Client, group_id: string, user_id: string): Member;
}
export declare namespace Member {
    interface OriginalInfo {
        Uin: number;
        UserName: string;
        NickName: string;
    }
    interface Info extends Contact.Info {
        user_id: string;
        group_id: string;
        nickname: string;
    }
}
