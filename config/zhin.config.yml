log_level: info

# 插件实例配置（Plugin Runtime 形态）：plugins.<instanceKey> 对象。
# 适配器 endpoint 在对应实例配置内声明，例如：
# plugins:
#   icqq:
#     endpoints:
#       - name: '123456789'        # 须先 icqq login；name 为 QQ 号
#         master: '你的用户ID'      # ⚠️ Endpoint Master（ask_user 安全确认目标）
plugins: {}

# AI 服务配置（可选）— 新格式：命名 providers + 显式 sdk + agents.zhin 绑定
# ai:
#   enabled: true
#   providers:
#     ds-main:
#       sdk: deepseek
#       apiKey: ${DEEP_SEEK_API_KEY}
#     ollama-local:
#       sdk: ollama
#       host: http://127.0.0.1:11434
#   agents:
#     zhin:
#       provider: ds-main
#       model: deepseek-chat
#       toolSearch: false
#       tools:
#         - bash
#         - read_file
#         - write_file
#         - web_search
#         - activate_skill
#   sessions:
#     maxHistory: 200
#     expireMs: 604800000
#     useDatabase: true
#   context:
#     enabled: true
#     maxRecentMessages: 100
#     summaryThreshold: 50
#     keepAfterSummary: 10
#     maxContextTokens: 4000
#   agent:
#     execSecurity: allowlist
#     execPreset: network
#     execApprovalMode: ask
#   trigger:
#     prefixes: ['#', 'AI:', 'ai:']
#     respondToAt: true
#     respondToPrivate: true
#     timeout: 60000
