# 🚀 Level 0: 零基础快速开始

> **⏱️ 预计时间**: 15 分钟  
> **🎯 目标**: 让你的第一个机器人跑起来！  
> **📋 前置要求**: 无需任何经验

---

## 📝 第1步: 安装准备 (2 分钟)

### 检查 Node.js 版本

打开终端（Windows 用户打开 CMD 或 PowerShell），输入：

```bash
node --version
```

✅ **期望结果**: 看到类似 `v18.17.0` 或更高版本

❌ **如果没有安装**: 访问 [nodejs.org](https://nodejs.org) 下载安装

---

## 🎯 第2步: 创建项目 (3 分钟)

### 一键创建

```bash
# 🌟 只需要一行命令
npm create zhin-app my-first-bot
```

### 交互式配置

脚手架会问你几个问题：

```
? 选择配置格式 › - Use arrow-keys. Return to submit.
❯ TypeScript  (推荐)
  JavaScript
  YAML
  JSON
```

**建议选择**: TypeScript （即使你不熟悉，也没关系）

```
? 选择运行时 › - Use arrow-keys. Return to submit.
❯ Node.js    (推荐)
  Bun
```

**建议选择**: Node.js

```
? Web 控制台用户名 › (默认是你的系统用户名)
```

**直接回车**或输入你喜欢的用户名

```
? Web 控制台密码 › (会生成随机密码)
```

**直接回车**或输入你的密码

> 💡 **重要**: 记住这个密码！它会保存在 `.env` 文件中

---

## 🚀 第3步: 启动机器人 (2 分钟)

### 进入项目并启动

```bash
cd my-first-bot
pnpm dev
```

✅ **期望看到**:

```
[INFO] Loading configuration...
[INFO] Starting app in development mode
[INFO] Plugin test-plugin loaded
[INFO] HTTP server started on http://localhost:8086
[INFO] Bot console started
[INFO] Application started successfully
```

🎉 **恭喜！你的机器人已经在运行了！**

---

## 💬 第4步: 测试机器人 (3 分钟)

### 在终端直接测试

现在，在终端直接输入消息：

```bash
> hello
```

✅ **应该看到机器人回复**:

```
< 你好！欢迎使用 Zhin 机器人框架！
```

### 试试其他命令

```bash
> status
< 🤖 机器人状态
  ⏱️ 运行时间: 1分钟30秒
  📊 内存使用: 45.23MB
  🔧 Node.js: v18.17.0
```

```bash
> roll
< 🎲 你掷出了 4 点！（6 面骰子）
```

```bash
> roll 20
< 🎲 你掷出了 15 点！（20 面骰子）
```

---

## 🌐 第5步: 访问 Web 控制台 (2 分钟)

### 打开浏览器

访问: `http://localhost:8086`

### 登录

- **用户名**: 你刚才设置的用户名
- **密码**: 你刚才设置的密码（或查看项目目录下的 `.env` 文件）

### 探索控制台

你会看到：

- 📊 **Dashboard**: 实时状态监控
- 🧩 **Plugins**: 插件列表和管理
- 📝 **Logs**: 实时日志输出
- ⚙️ **Settings**: 配置管理

---

## 🔥 第6步: 体验热重载 (3 分钟)

### 打开项目文件

用任何代码编辑器（VS Code、Sublime、Notepad++等）打开：

```
my-first-bot/src/plugins/example.ts
```

### 修改欢迎语

找到这一行：

```typescript
addCommand(new MessageCommand('hello')
  .action(() => {
    return '你好！欢迎使用 Zhin 机器人框架！'
  })
)
```

改成：

```typescript
addCommand(new MessageCommand('hello')
  .action(() => {
    return '嗨！我是你的专属机器人助手！✨'
  })
)
```

### 保存文件

按 `Ctrl + S` (Mac: `Cmd + S`) 保存

### 终端会显示

```
[INFO] File changed: src/plugins/example.ts
[INFO] Reloading plugin: example
[INFO] example.ts reloaded successfully in 180ms
```

### 立即测试

```bash
> hello
< 嗨！我是你的专属机器人助手！✨
```

🔥 **看到了吗？无需重启，修改立即生效！这就是热重载的魔力！**

---

## 🎓 你学到了什么

✅ **完成的成就**:
- [x] 成功安装和创建项目
- [x] 启动了第一个机器人
- [x] 发送并接收消息
- [x] 访问了 Web 控制台
- [x] 体验了热重载功能
- [x] 修改了机器人的行为

🎉 **恭喜你完成了 Level 0！**

---

## 🎯 下一步

现在你已经入门了，可以继续学习：

### Option 1: 添加新功能
试着在 `src/plugins/example.ts` 中添加一个新命令：

```typescript
addCommand(new MessageCommand('time')
  .action(() => {
    return `⏰ 现在时间：${new Date().toLocaleString()}`
  })
)
```

保存后测试：
```bash
> time
< ⏰ 现在时间：2024/1/15 下午3:25:30
```

### Option 2: 继续深入学习
- 📚 [Level 1: 基础应用](./level1-basics.md) - 学习命令和插件开发
- 📖 [完整学习路径](../guide/learning-path.md) - 系统化学习指南

### Option 3: 探索项目结构
打开这些文件看看：
- `zhin.config.ts` - 机器人配置
- `src/plugins/example.ts` - 示例插件
- `.env` - 环境变量（包含密码）

---

## ❓ 常见问题

### Q: 端口被占用怎么办？

```
Error: listen EADDRINUSE: address already in use :::8086
```

**解决方法**: 修改 `zhin.config.ts`，找到：

```typescript
http: {
  port: 8086,  // 改成其他端口，如 3000
  // ...
}
```

### Q: 忘记密码了？

打开项目目录下的 `.env` 文件，查看：

```
HTTP_USERNAME=your-username
HTTP_PASSWORD=your-password
```

### Q: 热重载不工作？

检查清单：
- [ ] 是否在 `dev` 模式运行？（不是 `start`）
- [ ] 文件是否保存？
- [ ] 文件是否在 `src/plugins/` 目录？
- [ ] 终端是否有错误信息？

### Q: 如何停止机器人？

在终端按 `Ctrl + C`

---

## 🆘 获取帮助

遇到问题？别担心！

1. 📖 查看 [完整文档](https://zhin.pages.dev)
2. 💬 在 [GitHub Discussions](https://github.com/zhinjs/zhin/discussions) 提问
3. 🐛 报告 Bug: [GitHub Issues](https://github.com/zhinjs/zhin/issues)

---

🎉 **太棒了！你已经掌握了 Zhin.js 的基础！**

**准备好继续学习了吗？** 👉 [开始 Level 1](./level1-basics.md)

