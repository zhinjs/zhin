import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderSuspense, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"代码约定","description":"","frontmatter":{"title":"代码约定"},"headers":[],"relativePath":"contributing/conventions.md","filePath":"contributing/conventions.md"}');
const _sfc_main = { name: "contributing/conventions.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="代码约定" tabindex="-1">代码约定 <a class="header-anchor" href="#代码约定" aria-label="Permalink to &quot;代码约定&quot;">​</a></h1><p>这些约定大多有对应的 harness 门禁（见<a href="./development.html">开发流程</a>），违反会直接红 CI。改动前先读根目录 <code>AGENTS.md</code>。</p><h2 id="typescript-与模块" tabindex="-1">TypeScript 与模块 <a class="header-anchor" href="#typescript-与模块" aria-label="Permalink to &quot;TypeScript 与模块&quot;">​</a></h2><ul><li>全仓库 TypeScript ESM。<strong>本地相对导入必须带 <code>.js</code> 扩展名</strong>：</li></ul><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { DisposeStack } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;./dispose.js&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;        </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// ✅</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { DisposeStack } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;./dispose&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;           </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// ❌</span></span></code></pre></div><ul><li>Node 侧源码放 <code>src/</code>，构建产物放 <code>lib/</code>；浏览器侧源码放 <code>client/</code>，产物放 <code>dist/</code>。</li><li>新增 workspace 包必须落在 <code>pnpm-workspace.yaml</code> 覆盖的目录内，并带独立 <code>package.json</code>。</li><li><code>pnpm-workspace.yaml</code> 的 <code>overrides</code> 承担大量安全版本抬升（undici、hono、tar、js-yaml、nodemailer 等），不要随手删改；新增依赖注意 <code>pnpm check:dependency-policy</code> 的约束。</li></ul><h2 id="useplugin-必须在模块顶层" tabindex="-1">usePlugin() 必须在模块顶层 <a class="header-anchor" href="#useplugin-必须在模块顶层" aria-label="Permalink to &quot;usePlugin() 必须在模块顶层&quot;">​</a></h2><p><code>usePlugin()</code>（<code>@zhin.js/core</code>）依赖 AsyncLocalStorage 定位调用方文件，只能在<strong>模块顶层</strong>调用，不能放进异步函数、回调工厂或延迟执行路径。门禁 <code>pnpm check:use-plugin-top-level</code> 会扫描 <code>plugins/{adapters,features,utils,games}</code> 和 <code>examples/{minimal-bot,test-bot}</code>。</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { usePlugin } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;zhin.js&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// zhin.js 根导出 re-export @zhin.js/core</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> plugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> usePlugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// ✅ 模块顶层</span></span></code></pre></div><p>约定式插件（<code>definePlugin</code> / <code>defineCommand</code> / <code>defineAgentTool</code>，来自 <code>zhin.js/plugin-runtime</code> 等子路径）在声明式 API 内不需要手动调 <code>usePlugin()</code>。</p><h2 id="getplugin-的禁用域" tabindex="-1">getPlugin() 的禁用域 <a class="header-anchor" href="#getplugin-的禁用域" aria-label="Permalink to &quot;getPlugin() 的禁用域&quot;">​</a></h2><p><code>getPlugin()</code>（<code>packages/im/core/src/plugin-context.ts</code>）从 AsyncLocalStorage 取当前插件实例，只允许在<strong>初始化/装配阶段</strong>调用：</p><ul><li>✅ 允许：模块顶层、register/init 函数内、注册命令/中间件/工具<strong>之前</strong>。</li><li>❌ 禁止：中间件、命令 <code>.action()</code>、工具 <code>.execute()</code>、Cron、事件回调、生命周期 <code>.on()</code> 等运行时路径。</li></ul><p>运行时回调应使用注册时捕获的 <code>plugin</code> / <code>root</code> 闭包引用。ALS 在跨 await、线程池、部分平台适配器回调中会丢失，运行时调用会抛 <code>getPlugin() must be called within a plugin context</code>。门禁：<code>pnpm check:get-plugin-runtime</code>。</p><h2 id="模块级状态-creategenerationstore" tabindex="-1">模块级状态：createGenerationStore <a class="header-anchor" href="#模块级状态-creategenerationstore" aria-label="Permalink to &quot;模块级状态：createGenerationStore&quot;">​</a></h2><p>插件热重载意味着同一份模块代码会被多个 generation 先后使用。裸的模块级 <code>let _x</code> 单例会让新一代读到上一代已释放的资源，或让旧代卸载时误清掉新代的值。统一用 <code>createGenerationStore&lt;T&gt;(name)</code>（<code>@zhin.js/plugin-runtime</code>）：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { createGenerationStore } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@zhin.js/plugin-runtime&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> dbStore</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> createGenerationStore</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Database</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;my-plugin-db&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// setup 阶段：provide 自动挂 context.lifecycle 反注册，</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 代际结束时该代的值被移除，上一代的值重新可见</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">dbStore.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">provide</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(context, db);</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 运行时路径（工具 execute、Cron、事件回调）：读最新 live 值</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> db</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> dbStore.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">use</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();      </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 无值时抛出含 store 名的错误</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> maybe</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> dbStore.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">tryUse</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 无值时返回 undefined</span></span></code></pre></div><ul><li>多代并存时栈顶（最新 live 注册）胜出，旧代先 dispose 不会误伤新代。</li><li><code>clear()</code> 仅供测试复位。</li><li>不要手写 <code>if (!x) throw new Error(&#39;... not initialized&#39;)</code>，<code>use()</code> 已经带这个语义。</li></ul><h2 id="ws-sse-端点-createendpointlifecycle" tabindex="-1">WS/SSE 端点：createEndpointLifecycle <a class="header-anchor" href="#ws-sse-端点-createendpointlifecycle" aria-label="Permalink to &quot;WS/SSE 端点：createEndpointLifecycle&quot;">​</a></h2><p>长连接端点（napcat、milky、onebot11/12 这类 WS/SSE 适配器）的 start/stop/重连/心跳统一走 <code>createEndpointLifecycle</code>（<code>@zhin.js/adapter</code>），不要手写状态机：</p><ul><li>状态机：<code>idle → connecting → open → reconnecting → open … → stopped / closed</code>。</li><li><code>start(connectFn)</code>：连接失败自动复位回 <code>idle</code> 且不武装重连。</li><li><code>stop()</code>：清全部定时器、调用强关函数、唤醒竞态等待，绝不重连。</li><li><code>handle.notifyClosed()</code>：对端断开时由适配器调用，仅在连接曾 open 时按指数退避 + jitter 重连。</li><li><code>startHeartbeat(fn, interval)</code>：心跳 + 看门狗，连续 N 轮无回包（未 <code>notifyHeartbeatAck()</code>）时主动强关，由 close 事件驱动重连。</li><li>退避参数可配：<code>initialIntervalMs</code>（默认 5000）、<code>multiplier</code>（默认 2）、<code>maxIntervalMs</code>（默认 60000）、<code>jitterMs</code>、<code>maxAttempts</code>（默认 Infinity）。</li></ul><p>适配器专有的逻辑（如 agent 注册/反注册）留在适配器侧，<code>start</code> 失败时要对称反注册。</p><h2 id="消息统一链路" tabindex="-1">消息统一链路 <a class="header-anchor" href="#消息统一链路" aria-label="Permalink to &quot;消息统一链路&quot;">​</a></h2><p>所有出站消息必须走统一链路，禁止旁路发送（门禁 <code>pnpm check:harness-paths</code>）：</p>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-136",
        class: "mermaid",
        graph: "flowchart%20LR%0A%20%20reply%5B%22Message.%24reply%20%2F%3Cbr%2F%3EAdapter.sendMessage%22%5D%20--%3E%20render%5B%22renderSendMessage%22%5D%0A%20%20render%20--%3E%20before%5B%22root%20plugin%3Cbr%2F%3Ebefore.sendMessage%22%5D%0A%20%20before%20--%3E%20endpoint%5B%22%E5%B9%B3%E5%8F%B0%20Endpoint%22%5D%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<p>跨平台出站（从一个平台发到另一个平台）用 <code>root.inject(adapter).sendMessage</code>，不要直接操作 Endpoint。</p><h2 id="host-token-模式" tabindex="-1">Host token 模式 <a class="header-anchor" href="#host-token-模式" aria-label="Permalink to &quot;Host token 模式&quot;">​</a></h2><p>HTTP Host（<code>@zhin.js/host-http</code>）不内置会话体系，统一用 Bearer token 鉴权：</p><ul><li>客户端请求带 <code>Authorization: Bearer &lt;token&gt;</code>，token 来自 <code>http.token</code> 配置；服务端用 <code>TokenRegistry</code>（<code>packages/host/http/src/token-registry.ts</code>）校验，<code>extractBearerToken</code> 解析头。</li><li>token 比对走 <code>timingSafeEqualString</code>（常数时间比较），不要手写 <code>===</code> 比对。</li><li>token 按 scope 分级（<code>ScopedTokenConfig</code> / <code>AuthScope</code>）：写操作要求 full scope，demo token 一律 403。</li><li>Remote Console 登录 = API Base URL + Bearer Token，没有账号密码概念。</li></ul><h2 id="测试约定" tabindex="-1">测试约定 <a class="header-anchor" href="#测试约定" aria-label="Permalink to &quot;测试约定&quot;">​</a></h2><ul><li>测试用 Vitest，配置在根 <code>vitest.config.ts</code>：<code>globals: true</code>（无需 import <code>describe</code>/<code>it</code>/<code>expect</code>）、<code>environment: &#39;node&#39;</code>、匹配 <code>**/*.test.ts</code>。</li><li>文件级隔离开启（<code>isolate: true</code>），避免 <code>vi.spyOn</code> / <code>vi.mock</code> 跨文件泄漏；写测试时不要依赖跨文件的全局状态。</li><li>覆盖率阈值（v8 provider）：lines 45% / branches 35%。</li><li><strong>数据库回归优先用真实 SQLite</strong>：<code>basic/database</code> 的测试用 Node 内置 <code>node:sqlite</code> 的 <code>DatabaseSync</code> 跑真实方言（需要 Node 22.5+，推荐 24+；版本不足时跳过），而不是 mock 掉 SQL 层。</li><li>单包测试优先 <code>pnpm --filter &lt;pkg&gt; test</code>；全量 <code>pnpm test</code>。</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("contributing/conventions.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const conventions = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  conventions as default
};
