import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderSuspense, ssrRenderComponent, ssrRenderStyle } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"示例速览","description":"","frontmatter":{},"headers":[],"relativePath":"examples/index.md","filePath":"examples/index.md"}');
const _sfc_main = { name: "examples/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="示例速览" tabindex="-1">示例速览 <a class="header-anchor" href="#示例速览" aria-label="Permalink to &quot;示例速览&quot;">​</a></h1><p>仓库 <code>examples/</code> 下维护四个分层递进的官方示例，全部开箱可跑（仓库根先 <code>pnpm install</code>）：</p><table tabindex="0"><thead><tr><th>示例</th><th>定位</th><th>启动</th><th>通道</th></tr></thead><tbody><tr><td><a href="#minimal-bot-stable-最小路径">minimal-bot</a></td><td><strong>Stable 最小路径</strong>，仅 IM，零外部依赖</td><td>根目录 <code>pnpm dev</code></td><td>终端</td></tr><tr><td><a href="#capabilities-bot-defineplugin-能力样板">capabilities-bot</a></td><td><code>definePlugin</code> 能力样板，插件 + <code>zhin.config.yml</code> 即应用</td><td><code>pnpm --filter capabilities-bot dev</code></td><td>Sandbox + Console</td></tr><tr><td><a href="#full-bot-l4-参考含-ai">full-bot</a></td><td><strong>L4 参考</strong>，含 AI 栈、语义记忆、MCP</td><td><code>pnpm dev:full</code></td><td>Sandbox + NapCat + KOOK</td></tr><tr><td><a href="#test-bot-维护者厨房水槽">test-bot</a></td><td>维护者厨房水槽，全平台全插件</td><td><code>pnpm dev:test</code></td><td>20+ 适配器</td></tr></tbody></table>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-82",
        class: "mermaid",
        graph: "flowchart%20LR%0A%20%20A%5Bminimal-bot%3Cbr%2F%3E%E4%BB%85%20IM%20%C2%B7%20%E6%9C%80%E5%B0%8F%5D%20--%3E%20B%5Bcapabilities-bot%3Cbr%2F%3E%E8%83%BD%E5%8A%9B%E6%A0%B7%E6%9D%BF%5D%0A%20%20B%20--%3E%20C%5Bfull-bot%3Cbr%2F%3EL4%20%C2%B7%20%E5%90%AB%20AI%5D%0A%20%20D%5Btest-bot%3Cbr%2F%3E%E5%8E%A8%E6%88%BF%E6%B0%B4%E6%A7%BD%3Cbr%2F%3E%E7%BB%B4%E6%8A%A4%E8%80%85%E4%B8%93%E7%94%A8%5D%20-.%20%E9%9D%9E%E5%85%A5%E9%97%A8%E6%A8%A1%E6%9D%BF%20.-%3E%20A%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<p>学习路径：<strong>minimal-bot → capabilities-bot → full-bot</strong>；test-bot 仅供维护者对照。</p><h2 id="minimal-bot-stable-最小路径" tabindex="-1">minimal-bot：Stable 最小路径 <a class="header-anchor" href="#minimal-bot-stable-最小路径" aria-label="Permalink to &quot;minimal-bot：Stable 最小路径&quot;">​</a></h2><p>最小可运行的 Plugin Runtime 项目：一个 <code>plugin.ts</code> + 约定目录（<code>adapters/</code> <code>commands/</code> <code>components/</code>）， Stable Features（adapter / command / component）由框架核心自动继承，<code>package.json#zhin</code> 的 <code>features</code> / <code>plugins</code> 均为空。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 仓库根</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">            # = pnpm --filter minimal-bot dev</span></span></code></pre></div><p>等到 <code>zhin&gt; </code> 提示符出现，在同一终端输入 <code>/hello</code> 或 <code>/card</code>（渲染 Satori 状态卡片）。 <code>Ctrl+C</code> 退出；编辑 <code>adapters/</code>、<code>commands/</code> 或 <code>components/</code> 下的文件会触发热重载。</p><p>要求 Node ≥22.6（22.6–22.17 由 CLI 自动带 <code>--experimental-strip-types</code>，22.18+ 原生 TypeScript）。</p><p>目录速览：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>minimal-bot/</span></span>
<span class="line"><span>├── plugin.ts                 # definePlugin() 入口</span></span>
<span class="line"><span>├── schema.json               # 配置契约</span></span>
<span class="line"><span>├── zhin.config.yml           # plugin / plugins 分层配置</span></span>
<span class="line"><span>├── adapters/terminal.ts      # stdin/stdout 终端 Endpoint</span></span>
<span class="line"><span>├── commands/hello.ts         # /hello</span></span>
<span class="line"><span>├── commands/card.ts          # /card → 组件渲染</span></span>
<span class="line"><span>└── components/status-card.ts # Satori 卡片组件</span></span></code></pre></div><h2 id="capabilities-bot-defineplugin-能力样板" tabindex="-1">capabilities-bot：definePlugin 能力样板 <a class="header-anchor" href="#capabilities-bot-defineplugin-能力样板" aria-label="Permalink to &quot;capabilities-bot：definePlugin 能力样板&quot;">​</a></h2><p><strong>一个插件 + 一个 <code>zhin.config.yml</code> 即可启动</strong>的能力展示样板：一个 <code>setup()</code> 调动全部常用能力面—— 配置视图、数据库表、定时任务、Agent 工具（可选降级）、主动出站、lifecycle 回收、Console 卡片 metadata， 并演示挂载 npm 包子插件（<code>@zhin.js/adapter-sandbox</code>）。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> examples/capabilities-bot</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">            # zhin runtime start</span></span></code></pre></div><p>启动后：</p><ul><li>Console 在 <code>http://127.0.0.1:18099</code>（token <code>capabilities-dev-token</code>），plugins 页可见 <code>Capabilities Bot</code> 卡片</li><li>已注册 <code>whoami</code> / <code>stats</code> 命令（后者演示数据库计数）</li><li>心跳定时任务每 5 分钟打一行日志；<code>pushOnBoot: true</code> 时向 Sandbox 私聊推一条上线消息</li></ul><p>能力与代码的逐项对照见该目录 <code>README.md</code>。</p><h2 id="full-bot-l4-参考-含-ai" tabindex="-1">full-bot：L4 参考（含 AI） <a class="header-anchor" href="#full-bot-l4-参考-含-ai" aria-label="Permalink to &quot;full-bot：L4 参考（含 AI）&quot;">​</a></h2><p>在 minimal-bot 之上叠加完整 AI 栈：<code>@zhin.js/agent</code>、语义记忆、MCP、A2A Agent Mesh， 以及 Sandbox + NapCat + KOOK 三适配器与 Skill / Tool / Page 等 Feature。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 仓库根已 pnpm install &amp;&amp; pnpm build</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> examples/full-bot</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cp</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .env.example</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .env</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # 填入模型 Provider Key 与 HTTP_TOKEN</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                # 或仓库根 pnpm dev:full</span></span></code></pre></div><ul><li>Host 默认 <code>http://127.0.0.1:8069</code>；Remote Console 的 API Base / Token 与 <code>.env</code> 中 <code>HTTP_TOKEN</code> 一致</li><li>验收清单与 NapCat/KOOK 实机步骤见该目录 <code>ACCEPTANCE.md</code></li></ul><h2 id="test-bot-维护者厨房水槽" tabindex="-1">test-bot：维护者厨房水槽 <a class="header-anchor" href="#test-bot-维护者厨房水槽" aria-label="Permalink to &quot;test-bot：维护者厨房水槽&quot;">​</a></h2><blockquote><p><strong>这不是入门模板，请勿复制为新项目的起点。</strong> 新项目用 <code>pnpm create zhin-app</code> 或 minimal-bot。</p></blockquote><p>挂载 20+ 平台适配器（Sandbox、ICQQ×5、QQ 官方、Slack、Discord、Telegram、KOOK、钉钉、飞书、 企业微信、LINE、Email、GitHub……）与全部功能插件（游戏中心、lottery、group-suite、rss、code-runner 等）， 用于维护者做多端回归。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;&amp; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> build</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # 仓库根</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev:test</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                # 或 cd examples/test-bot &amp;&amp; pnpm dev</span></span></code></pre></div><p>要求 Node ≥22.18；凭据读该目录 <code>.env</code>（模板见 <code>.env.example</code>）。 Console：打开 <code>https://console.zhin.dev</code>，API Base 填 <code>http://127.0.0.1:8086</code>，Token 填 <code>test-bot-dev-token</code>。</p><p>后台守护模式：<code>pnpm daemon</code>（<code>zhin runtime start --daemon</code>），<code>pnpm stop</code> 停止。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("examples/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
