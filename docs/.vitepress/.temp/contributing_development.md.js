import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderSuspense, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"开发流程","description":"","frontmatter":{"title":"开发流程"},"headers":[],"relativePath":"contributing/development.md","filePath":"contributing/development.md"}');
const _sfc_main = { name: "contributing/development.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="开发流程" tabindex="-1">开发流程 <a class="header-anchor" href="#开发流程" aria-label="Permalink to &quot;开发流程&quot;">​</a></h1><p>本页覆盖日常开发闭环：环境准备、构建与测试、harness 门禁、changeset 与发版、调试手段。</p><h2 id="环境准备" tabindex="-1">环境准备 <a class="header-anchor" href="#环境准备" aria-label="Permalink to &quot;环境准备&quot;">​</a></h2><ul><li>Node.js <code>^20.19.0</code> 或 <code>&gt;=22.12.0</code></li><li>pnpm 9（仓库锁定 <code>packageManager: pnpm@9.0.2</code>）</li></ul><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # 安装全部 workspace 依赖</span></span></code></pre></div><h2 id="常用命令" tabindex="-1">常用命令 <a class="header-anchor" href="#常用命令" aria-label="Permalink to &quot;常用命令&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        # 启动 examples/minimal-bot（Sandbox + Console），首次验证改动推荐它</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev:full</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # 启动 examples/full-bot（L4 参考）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev:test</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # 启动 examples/test-bot（维护者厨房水槽）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> build</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      # turbo 按 basic → packages → plugins 顺序构建全部包</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> test</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">       # 全量 Vitest</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> type-check</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> # tsc --noEmit（tsconfig.typecheck.json）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> lint</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">       # ESLint</span></span></code></pre></div><p>只验证单个包时优先用 <code>pnpm --filter &lt;pkg&gt; build|test</code>，不要默认跑全量构建。</p><p>改 CLI 或 create-zhin-app 之前，如果报找不到 <code>@zhin.js/scaffold-wizard</code>，先构建它（产物在 <code>lib/</code>，未构建时 Node 无法解析）：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @zhin.js/scaffold-wizard</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> build</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # 或 pnpm prepare:cli</span></span></code></pre></div><h2 id="harness-门禁-pnpm-check-all" tabindex="-1">harness 门禁（pnpm check:all） <a class="header-anchor" href="#harness-门禁-pnpm-check-all" aria-label="Permalink to &quot;harness 门禁（pnpm check:all）&quot;">​</a></h2><p><code>pnpm check:all</code> 运行 <code>scripts/check-all-harness.mjs</code>，串行执行 <strong>38 项检查</strong>（含 type-check、lint、单测），全部通过才算绿。CI 若另跑 coverage 作业，可设 <code>HARNESS_SKIP_TEST=1</code> 跳过其中的 <code>pnpm test</code>，避免双跑。</p><p>按职责分组如下（括号内是单项命令，均可单独运行）：</p><p><strong>质量基线</strong></p><table tabindex="0"><thead><tr><th>检查</th><th>说明</th></tr></thead><tbody><tr><td>Type Check（<code>pnpm type-check</code>）</td><td><code>tsc --noEmit</code></td></tr><tr><td>Lint（<code>pnpm lint</code>）</td><td>ESLint（.ts/.tsx）</td></tr><tr><td>Unit Tests（<code>pnpm test</code>）</td><td>全量 Vitest</td></tr><tr><td>Production Config（<code>pnpm check:prod</code>）</td><td>生产配置无调试代码</td></tr></tbody></table><p><strong>架构与依赖</strong></p><table tabindex="0"><thead><tr><th>检查</th><th>说明</th></tr></thead><tbody><tr><td>Architecture Layers（<code>pnpm check:architecture</code>）</td><td>分层依赖方向（basic → kernel → ai → core → agent → zhin）</td></tr><tr><td>Dependency Policy（<code>pnpm check:dependency-policy</code>）</td><td>用户项目脚手架依赖默认写 <code>latest</code></td></tr><tr><td>No Koa Import（<code>pnpm check:no-koa</code>）</td><td>插件不得直接 import koa</td></tr><tr><td>Install Size（<code>pnpm check:install-size</code>）</td><td>zhin.js IM 核心 production <code>node_modules</code> ≤ 10MB</td></tr></tbody></table><p><strong>API 快照与插件规范</strong></p><table tabindex="0"><thead><tr><th>检查</th><th>说明</th></tr></thead><tbody><tr><td>API Surface（<code>pnpm check:api-surface</code>）</td><td>public API surface 快照</td></tr><tr><td>Plugin Runtime API（<code>pnpm check:plugin-runtime-api</code>）</td><td>约定式插件运行时 API surface 快照</td></tr><tr><td>Plugin Spec（<code>pnpm check:plugin</code>）</td><td>插件符合标准规范</td></tr><tr><td>Plugin Agent Publish（<code>pnpm check:plugin-agent-publish</code>）</td><td>带 <code>agent/</code> 的插件发布清单（files、prepublishOnly、peer 依赖）</td></tr><tr><td>Publish Repository（<code>pnpm check:publish-repository</code>）</td><td>可发布包 <code>repository.url</code> 匹配 github.com/zhinjs/zhin（npm provenance）</td></tr><tr><td>Agent Tool Schema（<code>pnpm check:agent-tool-schema</code>）</td><td><code>agent/tools</code> inputSchema 与 defineAgentTool/execute 类型一致</td></tr><tr><td>No Package-Root skills/（<code>pnpm check:no-package-skills</code>）</td><td>插件包禁止顶层 <code>skills/</code>，须用 <code>agent/skills/*.md</code></td></tr></tbody></table><p><strong>IM 链路与运行时约定</strong></p><table tabindex="0"><thead><tr><th>检查</th><th>说明</th></tr></thead><tbody><tr><td>IM Send Path（<code>pnpm check:harness-paths</code>）</td><td>不得绕过 Adapter.sendMessage 统一链路</td></tr><tr><td>IM Session SSOT（<code>pnpm check:im-session-ssot</code>）</td><td>IM 场景/session 身份解析走 core SSOT</td></tr><tr><td>usePlugin Top-Level（<code>pnpm check:use-plugin-top-level</code>）</td><td><code>usePlugin()</code> 须在模块顶层</td></tr><tr><td>getPlugin Runtime（<code>pnpm check:get-plugin-runtime</code>）</td><td>运行时回调内禁止 <code>getPlugin()</code></td></tr><tr><td>Orchestration SSOT（<code>pnpm check:orchestration-ssot</code>）</td><td>编排任务状态须经 OrchestrationKernel</td></tr></tbody></table><p><strong>AI 层</strong></p><table tabindex="0"><thead><tr><th>检查</th><th>说明</th></tr></thead><tbody><tr><td>getModel Import Disambiguation（<code>pnpm check:get-model-imports</code>）</td><td>运行时代码用 getLlmTransportModel，不用歧义 getModel</td></tr><tr><td>Legacy AI Exports（<code>pnpm check:legacy-ai-exports</code>）</td><td><code>@zhin.js/ai</code> 不再导出 SessionManager 等符号</td></tr><tr><td>Provider Gateway（<code>pnpm check:provider-gateway</code>）</td><td>LLM 网关 sdk/contextWindow 预设契约</td></tr><tr><td>A2A Mesh（<code>pnpm check:a2a-mesh</code>）</td><td>禁止残留 MCP Agent Mesh v1 符号</td></tr></tbody></table><p><strong>适配器契约</strong></p><table tabindex="0"><thead><tr><th>检查</th><th>说明</th></tr></thead><tbody><tr><td>Rich Segment Adapters（<code>pnpm check:rich-segments</code>）</td><td>outboundRichSegmentPolicy 声明与契约测试</td></tr><tr><td>AI Outbound Adapters（<code>pnpm check:ai-outbound</code>）</td><td>aiOutboundExtensions 声明与契约测试</td></tr><tr><td>Interactive Segments（<code>pnpm check:interactive-segments</code>）</td><td>interactivePolicy 声明与契约测试</td></tr><tr><td>Segment Adapters（<code>pnpm check:segments</code>）</td><td>segment-mapper 契约（sandbox 必须达标）</td></tr></tbody></table><p><strong>文档一致性</strong></p><table tabindex="0"><thead><tr><th>检查</th><th>说明</th></tr></thead><tbody><tr><td>Doc Links（<code>pnpm check:doc-links</code>）</td><td>文档相对链接不断裂</td></tr><tr><td>Doc Orphans（<code>pnpm check:doc-orphans</code>）</td><td>站点 Markdown 都在侧栏或 allowlist</td></tr><tr><td>ADR Manifest（<code>pnpm check:adr-manifest</code>）</td><td>ADR README 与侧栏覆盖所有 ADR</td></tr><tr><td>README Exports（<code>pnpm check:readme-exports</code>）</td><td>README import 与包导出一致</td></tr><tr><td>Config Docs（<code>pnpm check:config-docs</code>）</td><td>配置文档与 DEFAULT_CONFIG 关键字段对齐</td></tr><tr><td>Install Tiers SSOT（<code>pnpm check:install-tiers-ssot</code>）</td><td>根 README Install tiers 表与 <code>docs/snippets/install-tiers.md</code> 一致</td></tr><tr><td>Adapter Docs Sync（<code>pnpm check:adapter-docs</code>）</td><td>平台适配器文档与 <code>plugins/adapters/*/README.md</code> 同步（修复用 <code>pnpm sync:adapter-docs</code>）</td></tr><tr><td>Platform Tiers SSOT（<code>pnpm check:platform-tiers-ssot</code>）</td><td>能力分档/适配器索引与 <code>scripts/adapter-meta.mjs</code> 一致</td></tr></tbody></table><p><strong>冒烟</strong></p><table tabindex="0"><thead><tr><th>检查</th><th>说明</th></tr></thead><tbody><tr><td>Stable Smoke（<code>pnpm check:stable</code>）</td><td>Sandbox + Agent 核心单测 + minimal-bot 契约</td></tr><tr><td>L4-CI（<code>pnpm check:l4-ci</code>）</td><td>L4 确定性子集（编排/记忆/full-bot 契约）；全量 <code>pnpm check:l4</code> 在 nightly 跑</td></tr></tbody></table><h2 id="changeset-工作流" tabindex="-1">changeset 工作流 <a class="header-anchor" href="#changeset-工作流" aria-label="Permalink to &quot;changeset 工作流&quot;">​</a></h2><p>仓库用 <a href="https://github.com/changesets/changesets" target="_blank" rel="noreferrer">changesets</a> 管理版本与 changelog（配置见 <code>.changeset/config.json</code>：<code>baseBranch: main</code>、<code>access: public</code>）。任何影响已发布包行为的改动都要记 changeset：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> release</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # = pnpm changeset，交互式选择受影响的包与 semver 级别，生成 .changeset/*.md</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bump</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      # = pnpm changeset version，消费 changeset、升版本号、写 CHANGELOG</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pub</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">       # = pnpm changeset publish，发布到 npm</span></span></code></pre></div><p>日常开发只需 <code>pnpm release</code> 提交 changeset 文件；<code>bump</code> 和 <code>pub</code> 由 CI 执行。</p><h2 id="发版-github-ci" tabindex="-1">发版（GitHub CI） <a class="header-anchor" href="#发版-github-ci" aria-label="Permalink to &quot;发版（GitHub CI）&quot;">​</a></h2><p>发版由 <code>.github/workflows/publish.yml</code> 驱动，push 到 <code>main</code> 即触发：</p>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-498",
        class: "mermaid",
        graph: "flowchart%20LR%0A%20%20push%5B%22push%20main%22%5D%20--%3E%20install%5B%22pnpm%20install%22%5D%20--%3E%20build%5B%22pnpm%20build%22%5D%0A%20%20build%20--%3E%20harness%5B%22pnpm%20check%3Aall%22%5D%0A%20%20harness%20--%3E%20init%5B%22init-new-packages%3Cbr%2F%3E%E9%A6%96%E6%AC%A1%E5%8F%91%E5%B8%83%E7%9A%84%E6%96%B0%E5%8C%85%22%5D%0A%20%20init%20--%3E%20action%5B%22changesets%2Faction%22%5D%0A%20%20action%20--%3E%7C%22%E6%9C%89%E6%9C%AA%E6%B6%88%E8%B4%B9%20changeset%22%7C%20pr%5B%22%E5%BC%80%20version%20PR%3Cbr%2F%3Epnpm%20bump%22%5D%0A%20%20action%20--%3E%7C%22main%20%E5%90%88%E5%B9%B6%20version%20PR%22%7C%20pub%5B%22pnpm%20pub%3Cbr%2F%3E%E5%8F%91%E5%B8%83%20npm%22%5D%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<p>PR 门禁在 <code>.github/workflows/ci.yml</code>（Node 22/24 矩阵），同样跑 <code>pnpm check:all</code>。</p><h2 id="调试" tabindex="-1">调试 <a class="header-anchor" href="#调试" aria-label="Permalink to &quot;调试&quot;">​</a></h2><ul><li><strong>日志级别</strong>：<code>zhin.config.yml</code> 的 <code>log_level</code> 调为 <code>debug</code>（默认 <code>info</code>），可看到框架内部细节日志。</li><li><strong>Console 日志页</strong>：运行期日志经 <code>DatabaseLogTransport</code> 写入 <code>SystemLog</code> 数据库模型，Remote Console 的 logs 页读取它。浏览器打开 <a href="https://console.zhin.dev" target="_blank" rel="noreferrer">console.zhin.dev</a>，填 API Base URL（如 <code>http://127.0.0.1:8086</code>）和 Bearer Token（<code>http.token</code> 配置）即可查看。</li><li><strong>单包调试</strong>：<code>pnpm --filter &lt;pkg&gt; test</code> 加 <code>-t &#39;&lt;用例名&gt;&#39;</code> 过滤用例；<code>pnpm test:watch</code> 进入 watch 模式。</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("contributing/development.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const development = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  development as default
};
