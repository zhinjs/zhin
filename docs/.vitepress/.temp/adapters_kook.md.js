import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@zhin.js/adapter-kook","description":"","frontmatter":{"title":"@zhin.js/adapter-kook","package":"@zhin.js/adapter-kook","tier":"Advanced"},"headers":[],"relativePath":"adapters/kook.md","filePath":"adapters/kook.md"}');
const _sfc_main = { name: "adapters/kook.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><div class="info custom-block"><p class="custom-block-title">文档同步</p><p>本页由 <a href="https://github.com/zhinjs/zhin/tree/main/plugins/adapters/kook/README.md" target="_blank" rel="noreferrer"><code>plugins/adapters/kook/README.md</code></a> 自动生成。请修改包内 README 后运行 <code>pnpm sync:adapter-docs</code>。</p></div><h1 id="zhin-js-adapter-kook" tabindex="-1">@zhin.js/adapter-kook <a class="header-anchor" href="#zhin-js-adapter-kook" aria-label="Permalink to &quot;@zhin.js/adapter-kook&quot;">​</a></h1><p>Zhin.js KOOK（开黑啦）适配器（Plugin Runtime），默认通过 <strong>WebSocket Gateway</strong>（<code>kook-client</code>）收发消息；可选 <strong>Webhook</strong> 模式经 <code>httpHostToken</code> 接收平台 POST 推送。</p><h2 id="功能" tabindex="-1">功能 <a class="header-anchor" href="#功能" aria-label="Permalink to &quot;功能&quot;">​</a></h2><ul><li>WebSocket Gateway 入站（默认；无需公网 HTTPS / host）</li><li>Webhook 入站（<code>connection: webhook</code> + <code>httpHostToken</code> + <code>verify_token</code>）</li><li>解析频道与私聊文本消息</li><li>出站 <code>send({ target, payload })</code> → KOOK KMarkdown（<code>channel:id</code> / <code>private:id</code>）</li><li>约定式 <code>defineAdapter</code> / <code>definePlugin</code>（无需 <code>usePlugin</code>）</li></ul><h2 id="安装" tabindex="-1">安装 <a class="header-anchor" href="#安装" aria-label="Permalink to &quot;安装&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @zhin.js/adapter-kook</span></span></code></pre></div><h2 id="plugin-runtime" tabindex="-1">Plugin Runtime <a class="header-anchor" href="#plugin-runtime" aria-label="Permalink to &quot;Plugin Runtime&quot;">​</a></h2><ul><li><code>@zhin.js/adapter</code> — 约定式 <code>adapters/kook.ts</code>（<code>defineAdapter</code>）</li><li><code>@zhin.js/core</code> — <code>messageGatewayToken</code> 入站/出站</li><li><code>@zhin.js/host-http</code> — Webhook 模式 POST 路由（WebSocket 不需要）</li><li><code>@zhin.js/plugin-runtime</code> — <code>plugin.ts</code>（<code>definePlugin</code>）</li><li>配置经插件 <code>schema.json</code> 落到 <code>plugins.&lt;instanceKey&gt;</code></li><li><strong>WebSocket 路径无需</strong> <code>@zhin.js/host-http</code> / <code>@zhin.js/host-router</code></li></ul><p>入站：<code>gateway.receive({ adapter, target: &#39;channel:…&#39;|&#39;private:…&#39;, content, sender, metadata })</code><br> 出站：<code>send({ target, payload })</code> → <code>sendChannelMsg</code> / <code>sendPrivateMsg</code></p><h2 id="前置条件" tabindex="-1">前置条件 <a class="header-anchor" href="#前置条件" aria-label="Permalink to &quot;前置条件&quot;">​</a></h2><table tabindex="0"><thead><tr><th>要求</th><th>说明</th></tr></thead><tbody><tr><td><strong>Bot Token</strong></td><td>在 <a href="https://developer.kookapp.cn/" target="_blank" rel="noreferrer">KOOK 开发者平台</a> 创建应用并获取</td></tr><tr><td><strong>邀请入服</strong></td><td>将机器人邀请到目标服务器，并授予查看频道、发送消息等权限</td></tr><tr><td><strong>WebSocket（默认）</strong></td><td><code>kook-client</code> 正向连接；无需公网 URL</td></tr><tr><td><strong>Webhook</strong></td><td>需公网 HTTPS + Host <code>httpHostToken</code>；与 WebSocket 互斥</td></tr><tr><td><strong>host-http</strong></td><td>仅 Webhook 模式需要</td></tr></tbody></table><p>必填字段（<code>endpoints[i]</code>）：<code>name</code>、<code>token</code>。</p><h2 id="最小配置" tabindex="-1">最小配置 <a class="header-anchor" href="#最小配置" aria-label="Permalink to &quot;最小配置&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># zhin.config.yml（Plugin Runtime）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">plugins</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  kook</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # connection: websocket   # 默认</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    endpoints</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">name</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">my-kook-bot</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">        token</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">\${KOOK_TOKEN}</span></span></code></pre></div><p>根插件 <code>zhin.plugins</code>（或项目图）需引用 <code>@zhin.js/adapter-kook</code>（<code>instanceKey: kook</code>）。</p><h2 id="环境变量" tabindex="-1">环境变量 <a class="header-anchor" href="#环境变量" aria-label="Permalink to &quot;环境变量&quot;">​</a></h2><table tabindex="0"><thead><tr><th>变量</th><th>说明</th></tr></thead><tbody><tr><td><code>KOOK_TOKEN</code> / <code>KOOK_BOT_TOKEN</code></td><td>Bot Token</td></tr><tr><td><code>KOOK_BOT_NAME</code></td><td>可选，默认 endpoint 名</td></tr><tr><td><code>KOOK_VERIFY_TOKEN</code></td><td>Webhook 模式 verify token</td></tr><tr><td><code>KOOK_ENCRYPT_KEY</code></td><td>可选，Webhook 消息加密密钥</td></tr><tr><td><code>KOOK_WEBHOOK_PATH</code></td><td>可选，默认 <code>/kook/webhook</code></td></tr></tbody></table><h2 id="webhook" tabindex="-1">Webhook <a class="header-anchor" href="#webhook" aria-label="Permalink to &quot;Webhook&quot;">​</a></h2><p>在 KOOK 开发者后台选择 <strong>WebHook</strong> 连接模式，Callback URL 指向 Host 暴露的公网地址（建议在 URL 加 <code>?compress=0</code> 便于调试）。</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">plugins</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  kook</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    connection</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">webhook</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    webhookPath</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/kook/webhook</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    endpoints</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">name</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">my-kook-bot</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">        token</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">\${KOOK_TOKEN}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">        verify_token</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">\${KOOK_VERIFY_TOKEN}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        # encrypt_key: \${KOOK_ENCRYPT_KEY}   # 启用消息加密时必填</span></span></code></pre></div><p>Host 需注入 <code>httpHostToken</code>。Challenge（<code>type: 255</code>）会校验 <code>verify_token</code> 并回显 <code>challenge</code>；普通事件经 <code>gateway.receive</code> 入站，出站仍走 KOOK HTTP API。</p><h2 id="ai-工具-skill" tabindex="-1">AI 工具（Skill） <a class="header-anchor" href="#ai-工具-skill" aria-label="Permalink to &quot;AI 工具（Skill）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>类别</th><th>路径</th></tr></thead><tbody><tr><td>Permit 词汇</td><td><code>agent/PERMITS.md</code></td></tr><tr><td>平台工具</td><td><code>agent/tools/</code>（角色、黑名单等）</td></tr><tr><td>技能说明</td><td><code>agent/skills/kook.md</code></td></tr></tbody></table><h2 id="平台权限-platform-permit" tabindex="-1">平台权限（platform permit） <a class="header-anchor" href="#平台权限-platform-permit" aria-label="Permalink to &quot;平台权限（platform permit）&quot;">​</a></h2><p>platform permit checker 由 <code>plugin.ts</code> 的 generation 生命周期注册；CapabilityIngress 与 ToolSystem 统一经 Core <code>canAccessTool()</code> 消费工具的 platform permit 声明。</p><h2 id="迁移后出站能力变化" tabindex="-1">迁移后出站能力变化 <a class="header-anchor" href="#迁移后出站能力变化" aria-label="Permalink to &quot;迁移后出站能力变化&quot;">​</a></h2><p>迁移到 Plugin Runtime 后，出站统一经 <code>messageGatewayToken</code> 渲染为文本后发送（<code>sendChannelMsg</code> / <code>sendPrivateMsg</code>，KMarkdown 文本）。旧 Adapter 的富媒体出站能力（图片 / 卡片消息 / 附件等多模态 segment 直发）暂未迁移，当前出站等价于纯文本（KMarkdown）。如需发送卡片或附件，可直接使用 endpoint 上的 KOOK OpenAPI 封装（<code>getRoleList</code> 等同款 client）作为逃生舱。</p><h2 id="许可证" tabindex="-1">许可证 <a class="header-anchor" href="#许可证" aria-label="Permalink to &quot;许可证&quot;">​</a></h2><p>MIT License</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adapters/kook.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const kook = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  kook as default
};
