# Conventions

Use the repository code style, minimal focused changes, typed public APIs, meaningful validation, and `type(scope): description` commits. PR descriptions must state the change, linked issue, and verification. Record recurring implementation mistakes here.
