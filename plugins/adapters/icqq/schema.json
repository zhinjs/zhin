{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "master": {
      "type": [
        "string",
        "number"
      ],
      "description": "框架 master QQ uin（/approve、AI/工具权限）；endpoints[i].master 可逐项覆盖"
    },
    "trusted": {
      "type": "array",
      "items": {
        "type": [
          "string",
          "number"
        ],
        "description": "Trusted QQ uin"
      },
      "description": "框架 trusted QQ uin 列表（弱于 master）"
    },
    "autoReconnect": {
      "type": "boolean",
      "default": true
    },
    "outboundMedia": {
      "type": "string",
      "enum": [
        "file",
        "base64"
      ],
      "description": "file=local path (same host as daemon); base64=CQ base64:// (default when rpc set)"
    },
    "endpoints": {
      "type": "array",
      "description": "多账号：一个插件实例挂多个 endpoint。每项与顶层字段同构（id 必填，其余覆盖顶层）",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "master": {
            "type": [
              "string",
              "number"
            ],
            "description": "本 endpoint 的框架 master QQ uin；覆盖顶层 master"
          },
          "trusted": {
            "type": "array",
            "items": {
              "type": [
                "string",
                "number"
              ],
              "description": "Trusted QQ uin"
            },
            "description": "本 endpoint 的 trusted 列表"
          },
          "rpc": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "host": {
                "type": "string",
                "description": "RPC host"
              },
              "port": {
                "type": "number",
                "description": "RPC port"
              },
              "token": {
                "type": "string",
                "description": "RPC token"
              }
            },
            "required": [
              "host",
              "port",
              "token"
            ]
          },
          "id": {
            "type": "string",
            "description": "QQ uin (must match icqq login account)"
          }
        },
        "required": [
          "id"
        ]
      }
    },
    "commandPrefix": {
      "type": "string",
      "default": "",
      "description": "命令前缀（默认 '' 无前缀，任意文本按命令匹配；如 '/' 要求 / 开头）。endpoints[i] 可逐项覆盖"
    }
  },
  "required": [
    "master",
    "endpoints"
  ]
}
