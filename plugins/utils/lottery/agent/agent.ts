import { defineAgent } from '@zhin.js/agent';

export default defineAgent({
  description: 'Multi-game lottery analysis pipeline: sync, review, recommend, publish.',
  keywords: ['lottery', 'caipiao', 'kl8', 'ssq'],
});
