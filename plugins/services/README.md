# plugins/services/

**可选**服务类插件目录（第三方或实验性）。

内置 Host 栈见 `packages/host/`。

| 插件 | 说明 |
|------|------|
| `@zhin.js/service-activity-feedback` | 订阅 AI 生命周期事件，按根级 `activityFeedback` 配置驱动各适配器反馈（queued / active / thinking） |
